
  # Component-based app structure

  This is a code bundle for Component-based app structure. The original project is available at https://www.figma.com/design/4q4wrUvBtoWa7xHKWamkQI/Component-based-app-structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  